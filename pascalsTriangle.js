/**
 * @param {number} numRows
 * @return {number[][]}
 */
var generate = function(numRows) {
    const x = 1
    const y = 1
     return Math.pow(x+y,numRows-1)
    
};


function generatePalindromeArrayWithSum(targetSum) {
    for (let index = 0; index < targetSum; index++) {
        let palArray = [1]
        for (let j = 0; j <=index ; j++) {
            palArray[j] = 
            
        } 
        console.log(palArray);
    }
   
  }
  
 generatePalindromeArrayWithSum(generate(5))

 
  

  